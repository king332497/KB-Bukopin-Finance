# Baseline rollback checkpoint

Visual source of truth: `docs/baseline/preview (3)(1).html`.

Rules:
- Preserve closed/default visual states before calibration.
- Technical changes must be tested for visual drift.
- No OTP/PIN/CVV/password/banking credential collection.
- Footer identifies this build as an information portal rather than an official bank property.

import { q, qa } from '../core/dom.js';
export function initReadiness(){const checks=qa('#cek-awal input[type="checkbox"]'),result=q('#eligibilityResult');if(!checks.length||!result)return;const update=()=>{const done=checks.filter(c=>c.checked).length;if(done===0)result.textContent='Tandai checklist untuk melihat tingkat kesiapan informasi Anda.';else if(done<=2)result.textContent='Sebagian informasi dasar sudah tersedia. Lengkapi poin lain agar konsultasi lebih efisien.';else if(done===3)result.textContent='Informasi dasar hampir lengkap. Periksa kembali dokumen pendukung sebelum menggunakan kanal resmi.';else result.textContent='Informasi dasar terlihat lengkap untuk memulai konsultasi. Ini bukan keputusan atau persetujuan kredit.';};checks.forEach(c=>c.addEventListener('change',update));}

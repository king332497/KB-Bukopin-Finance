# KB Finance — Baseline Stable

This build restores `preview (3)(1).html` as the visual checkpoint and extracts CSS/JS into maintainable modules.

## Run
Use a local static server, e.g. `python -m http.server 8080`, then open `http://localhost:8080/`.

## Visual rule
Do not change layout, typography, spacing, header, hero, navigation, or major proportions without baseline/reference screenshot comparison.
